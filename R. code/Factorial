Fact <- function(n) {
  if(n == 0 || n == 1){
    return(1)
  }else{
   return(n * Fact(n - 1)) 
  }
}

cat("Enter number: ");
input <- 10

result <- Fact(input)
cat("The factorial of: ", input, " is the number:", result);
