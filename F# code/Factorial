open System

let rec Factorial n =
  if n <= 1 then 1
  else n * Factorial(n - 1)

printfn "Enter user input: "
let input = System.Console.ReadLine()
let number = int input

let result = Factorial number
printfn "The factorial of the number %d is: %d" number result
